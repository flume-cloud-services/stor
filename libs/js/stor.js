import { textChangeRangeIsUnchanged } from "typescript";

// Stor.js API wrapper
export default class Stor {
    constructor(link, token) {
        this.link = link
        this.token = token
    } 
}